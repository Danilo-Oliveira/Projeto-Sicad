git init

git add .

git config --global user.email "sdanilo109@gmail.com"

git config --global user.name "Danilo Olivera"

git commit -m "Primeiro commit"

git remote add origin https://github.com/Danilo-Oliveira/CRUD_Python-SQL.git

git push -u origin master

git push -f origin master ** caso n funcionar o " git push -u origin master "